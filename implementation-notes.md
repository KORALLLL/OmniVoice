# Implementation Notes

## 2026-08-02 - Balalaika memorization and hard-number validation

- Decision: Execute the approved checkpoint-isolated design through the 12-task subagent-driven implementation plan.
- Assumption: Balalaika text is authoritative only from `rover_punctuated_accented`, joined by `source_relative_path`; TAR-side text and quality metadata do not influence selection.
- Tradeoff: Prefer 3–12 second references but permit clips over 12 seconds only as a deterministic fallback, with no hard upper limit.
- Constraint: Initial real GPU work is one path with no automatic retry and one shared one-hour monotonic deadline.
- Validation: Pre-implementation baseline passed 59 tests with six existing warnings in the worktree virtual environment; the system Python is not a valid test environment because it lacks the required Transformers API.
- Decision: Keep GigaAM ONNX GPU and W&B dependencies in the `validation` optional extra so default OmniVoice installs remain unchanged.
- Validation: Task 1 dependency test passed; `uv.lock` resolves the exact validation packages, and independent review approved the diff with no findings.
- Decision: Stream the 4.075M-row sidecar through bounded deterministic priority pools, with pool sizes 512, 2,048, and 8,192 for CPU-only candidate recovery.
- Tradeoff: Content-addressed converted WAV names preserve an existing published manifest if a later preparation attempt fails before atomic replacement.
- Validation: Task 2 passed five focused tests using real miniature TAR/MP3 fixtures, FFmpeg mono 24 kHz PCM16 conversion, malformed audio, metadata invariance, and >12-second fallback; independent review found no issues. Full proprietary-sidecar execution remains for Task 12.
- Decision: Treat `steps` as the immutable scheduler horizon and `stop_after_step` only as an invocation boundary; resumed segments reconstruct their within-epoch data cursor while restoring Python, NumPy, CPU, and CUDA RNG afterward.
- Changed: Task 3 review exposed replayed epoch prefixes, rank-local evaluation deadlock risk, zero-count evaluation, changed natural-completion behavior, and weak config typing. Fix round 1 added cursor replay, synchronized evaluation-stage errors, guaranteed teardown, global loss sum/count reduction, bounded-only final evaluation, and strict controls.
- Validation: Task 3 fix gate passed 72 control/LoRA/checkpoint tests and Ruff; a real bounded two-rank CPU Gloo fault reached both ranks without hanging. Scoped re-review marked all five findings addressed with no new breakage. Live NCCL/eight-GPU execution remains for Task 12.
- Decision: Run the four-sample memorization experiment as one staged, atomic artifact publication under a shared monotonic deadline. Require the exact fresh checkpoint that produced the qualifying loss and strictly validate the rank-64 broad LoRA target contract before training.
- Changed: Task 4 review hardened subprocess-tree termination, selected-row and copied-audio integrity, exact 25-step loss-history cadence, injected-loader process startup cleanup, and checkpoint/outcome consistency. A final compatibility correction permits the same member basename in different shards and different positive per-row schema versions while preserving uniqueness of shard-qualified source paths, IDs, resolved audio paths, and both recorded hashes.
- Validation: Task 4 passed 82 focused tests plus Ruff and diff checks. Independent fix-round review found no new breakage. The real four-utterance GPU memorization run and audio inspection artifacts remain intentionally deferred to Task 12.
- Decision: Treat the hard-number assignment JSONL as a self-validating immutable artifact, not merely a serialization of trusted upstream objects. Publication rechecks exact pinned dataset coordinates, fixed generation settings/seeds, canonical normalized IDs, twenty stable references at 100 prompts each, canonical round-robin mapping, and reference audio/hash/provenance integrity.
- Changed: The pinned dataset uses integer IDs, so loading normalizes them to canonical strings. Persisted assignments must already carry canonical string IDs; leading/trailing whitespace, non-string IDs, and normalized collisions are rejected before sorting. Valid duplicate member basenames across distinct shards and nonuniform positive schema versions remain supported.
- Validation: Task 5 passed 51 focused tests and Ruff after two review rounds. A read-only load of revision `57b964492ccfcedd6a24d0225ef4b7d3697ffdca` confirmed exactly 2,000 rows and 2,000 unique normalized IDs; independent re-review found no new breakage.
- Decision: Preserve the pinned benchmark's normalization, edit traceback priority, and two-stage number-span mapping, including its mapped-range boundary behavior, while exposing immutable raw S/D/I/C/N counts for utterance words/chars and number words/chars.
- Changed: Corpus CER now sums character errors over reference characters directly; it never reuses word denominators or averages per-utterance rates. Missing number spans are excluded from number aggregates without excluding their utterance metrics.
- Validation: Task 6 passed 16 focused tests and Ruff. Independent review found no issues and matched normalization/alignment/span behavior to the pinned evaluator across randomized cases; overall and sorted per-category aggregation use the correct family-specific micro denominators.
- Decision: Use one durable rank-local JSONL ledger writer per process. Every update writes a same-directory temporary file, fsyncs it, atomically replaces the destination, fsyncs the parent directory, and reloads authoritative disk state before propagating any failure so subsequent upserts cannot clobber a post-replace record.
- Changed: Ledger loading is strict JSON: nonfinite constants and duplicate members at any depth are rejected. Deep copies isolate nested caller and accessor values from live state. Cross-process locking is intentionally unnecessary under the one-writer-per-rank stage design.
- Validation: Task 7 passed 31 focused tests and Ruff after review hardening. Independent re-review confirmed pre/post-replace failure consistency, recursive strict parsing, nested mutation isolation, duplicate-safe canonical merge, SHA/ASR completion semantics, and exact 1,999/2,000 coverage rejection.
- Changed: Task 8 review round 2 closes lifecycle and model-provenance races. SIGTERM is rechecked through final serialization/output; synchronization, process-group destruction, garbage collection, and CUDA release are daemon-thread bounded. Lifecycle failures atomically correct an existing rank summary with structured stage/type/message details before preserving and propagating the primary exception.
- Decision: A LoRA validation source is identified by the composite of its recursive checkpoint hash and its recorded base model resolved to an immutable Hub snapshot. Inference loads that exact local base snapshot through a backward-compatible override and fingerprints the adapter both before CUDA work and after adapter loading.
- Validation: Task 8 round 2 passed the 134-test Task 5/7/8 plus LoRA-loading regression suite and scoped Ruff; the only warning is the known pydub/audioop deprecation. Real Hub/GPU/NCCL execution remains assigned to Task 12.
- Changed: Task 8 review round 3 moves the only owned model reference into a holder that is cleared inside the bounded release worker, so destructor work is covered by the cleanup deadline. Synchronization and release share one monotonic deadline; a timeout immediately corrects durable state, publishes one flushed incomplete result, and hard-exits the rank process without starting another CUDA/NCCL lifecycle action.
- Decision: Final synthesis output is one atomic SIGTERM boundary. Pending/captured SIGTERM is sampled after masking and before serialization; SIGTERM arriving during the single serialization/output commit is deferred until the authoritative document is complete, eliminating contradictory retry documents.
- Validation: Real subprocess tests cover blocked synchronization, model destruction, and CUDA release. Each exits code 2 within the post-ready bound, writes one incomplete payload and durable summary, and cannot execute abandoned-worker or post-exit markers. The focused Task 8 plus LoRA-loader suite passed 56 tests; the full Task 5/7/8 gate passed 133 tests, with only the known pydub/audioop warning.
- Changed: Task 8 review round 4 clears completed exception traceback frames before bounded release, removing retained `**kwargs` model references while preserving traceback code locations, exception type/message, and cleanup notes for re-raise.
- Decision: Once source context exists, every primary rank-stage failure is durably represented before synchronization or release. A fallback incomplete summary records run/rank/step/source, nullable unknown coverage, and structured primary error; a later cleanup timeout is recorded separately without replacing the primary diagnostic.
- Validation: Error-path subprocesses cover a blocking destructor retained by a synthesizer traceback and an immediate synthesis failure followed by blocked synchronization. Both produce one durable/payload error envelope and bounded code-2 hard exit with no later marker. Focused Task 8 plus LoRA-loader passed 60 tests; full Task 5/7/8 passed 137 tests, with only the known pydub/audioop warning.
- Changed: Task 8 review round 5 traverses the complete primary exception graph (cause, context, exception groups, and cycles) inside the bounded release worker. It clears only completed frames before model-holder/CUDA release, so partial loader models and nested traceback references are released under the same deadline while useful traceback and chaining structure remain available for re-raise.
- Decision: A prior rank summary is reusable only when its run ID, step, rank, and canonical model-source identity exactly match the current invocation and its schema/count invariants are valid. Malformed or stale summaries are ignored; a current-source fallback may retain independently known expected coverage but never inherits completed/generated/skipped/failed counts from another context.
- Validation: New real-process cases cover a blocking destructor reachable only through a nested cause and a partially constructed loader model before holder assignment. The focused synthesis/CLI/LoRA gate passed 73 tests and the full Task 5/7/8 gate passed 150 tests, with only the known pydub/audioop warning.
